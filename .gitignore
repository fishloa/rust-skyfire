# Rust build artifacts
/target

# Delegate scratch workspace (briefs are regenerated from the template; logs are local)
/.delegate/*.log
/.delegate/brief-*.txt

# Spec PDFs are NOT vendored (copyright / large binaries). Curated md in specs/md/ IS tracked.
specs/pdf/
specs/txt/

# Web shell deps
web/node_modules/
web/dist/
web/pkg/

# Crush / local tool state
/.crush
/.delegate/*.sid

# Generated HLS remux artifacts (probe-only, regenerate via ffmpeg -c copy)
fixtures/gulli.m3u8
fixtures/g*.ts
fixtures/gulli-prog.m3u8
fixtures/p*.ts
web/test-results/
web/playwright-report/
.playwright-mcp/

.delegate/

.DS_Store
