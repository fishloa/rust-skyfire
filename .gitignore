# Rust build artifacts
/target

# Delegate scratch workspace (briefs are regenerated from the template; logs are local)
/.delegate/*.log
/.delegate/brief-*.txt

# Spec PDFs are NOT vendored (copyright / large binaries). Curated md in specs/md/ IS tracked.
specs/pdf/
specs/txt/

# Web shell deps
web/node_modules/
web/dist/
web/pkg/

# Crush / local tool state
/.crush
/.delegate/*.sid

# Generated HLS remux artifacts (probe-only, regenerate via ffmpeg -c copy)
fixtures/gulli.m3u8
fixtures/g*.ts
fixtures/gulli-prog.m3u8
fixtures/p*.ts
web/fixtures-hls/
web/test-results/
web/playwright-report/
.playwright-mcp/

.delegate/

.DS_Store

# Captured live TS streams (large, moved from zenith .ts-captures — never committed)
/.ts-captures/

packages/*/pkg/
packages/*/*.tgz

# SDD scratch (subagent ledger/reports)
.superpowers/
.claude/
crates/skyfire-wasm/packages/
crates/skyfire-wasm/web/
/.ts-captures/progressive/
packages/*/node_modules/
packages/*/bun.lock
.worktree/
